name: ${PROJECT_NAME}_frontend

services:
  app:
    build:
      context: .
      target: app
    volumes:
      - .:/app
      - /app/node_modules
    command: npm run dev -- --host
    env_file:
      - .env.docker
    environment:
      - CHOKIDAR_USEPOLLING=true
    ports:
      - "5173:5173"